(require 'org)

;; http://carlhu.com/orgmode for more goodness later
(setq org-startup-indented t)
(setq org-indent-mode t)
(setq-default indent-tabs-mode nil)    ; use only spaces and no tabs

(provide 'org-setup)
